# This is an example neoforge.mods.toml file. It contains the data relating to the loading mods.
# There are several mandatory fields (#mandatory), and many more that are optional (#optional).
# The overall format is standard TOML format, v0.5.0.
# Note that there are a couple of TOML lists in this file.
# Find more information on toml format here:  https://github.com/toml-lang/toml

modLoader="javafml"

loaderVersion="${loader_version_range}"

license="${mod_license}"


issueTrackerURL="https://github.com/JuniKnytt/CreateRailGrinding/issues"

# A list of mods - how many allowed here is determined by the individual mod loader
[[mods]]

modId="${mod_id}"


version="${mod_version}"


displayName="${mod_name}"

displayURL="github.com/JuniKnytt/CreateRailGrinding"

logoFile="icon.png"

# A text field displayed in the mod UI
#credits="" #optional


authors="${mod_authors}"
description='''${mod_description}'''

# The [[mixins]] block allows you to declare your mixin config to FML so that it gets loaded.
[[mixins]]
config="${mod_id}.mixins.json"

# The [[accessTransformers]] block allows you to declare where your AT file is.
# If this block is omitted, a fallback attempt will be made to load an AT from META-INF/accesstransformer.cfg
#[[accessTransformers]]
#file="META-INF/accesstransformer.cfg"

# The coremods config file path is not configurable and is always loaded from META-INF/coremods.json

# A dependency - use the . to indicate dependency for a specific modid. Dependencies are optional.
[[dependencies.${mod_id}]]
    modId="neoforge"
    type="required"
    versionRange="[${neo_version},)"
    ordering="NONE"
    side="BOTH"

# Here's another dependency
[[dependencies.${mod_id}]]
    modId="create"
    type="required"
    versionRange="[6.0.8,6.1.0)"
    ordering="NONE"
    side="BOTH"

# Sable (Create Aeronautics' sublevel/physics engine). Optional — runtime compat only.
# AFTER ordering ensures Sable's SubLevelContainer is registered before our rail-grind code
# would ever query it via the compat shim. The shim itself is gated by Mods.SABLE.isLoaded()
# at every call site, so this entry just hints the load order; the mod runs fine standalone.
[[dependencies.${mod_id}]]
    modId="sable"
    type="optional"
    versionRange="[1.2.2,)"
    ordering="AFTER"
    side="BOTH"


